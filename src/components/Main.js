import React from "react";
import styled from "styled-components";

const Main = (props) => {
    return (
        <Container>
            <div>Main</div>
        </Container>
    );
};

const Container = styled.div``;



export default Main;